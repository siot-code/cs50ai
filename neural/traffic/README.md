First test based on lecture example gave during training 87% accuracy at its apex, but during the terst phase it went up to 97%. Apparently its due to the dropout nodes, having all nodes active during the testing the model is more confident.

Training with minimum dropout make the training end with a higher accuracy

I have tested the model with .1 dropout training on a stop sign and it has failed

Incrementing EPOCH is the most crucial factor!

Hidden layers nodes have quite an impact as well

it is yet really important to train the model on the most diverse data set